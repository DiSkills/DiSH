name: DiShell Tests

on:
  push:
    branches:
      - master
      - 'releases/**'
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Make test
        run: make test
