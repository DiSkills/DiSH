name: DiShell Tests

on:
  push:
    branches:
      - master
      - 'releases/**'
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Unity Test Framework
        run: make lib/unity

      - name: Make tests
        run: make tests
